import * as types from '../types' // 取出types所有的抛出

const state = {
  isLogin: false,
  userInfo: {}
}
const mutations = {
}
const actions = {
}
const getters = {
}

export default {
  state,
  getters,
  mutations,
  actions
}
